 use Data::Dumper;
 
 my %db_schema_versions = (
            1 => [
'create table diffs (diff integer primary key not null, date_begin datetime not null, date_end datetime not null, message text);',
'create table options (name text primary key not null, value text);',
'create table files (file_id integer autoincrement primary key not null, diff integer not null, path_id integer not null, localfile_id integer, retrieval_date datetime not null, owner text, group text, mode text, mdate not null datetime, type text not null, size integer not null, target text );',
'create table paths (path_id integer autoincrement primary key not null, path text not null);',
'create table localfiles (localfile_id integer autoincrement primary key not null, path text not null, size integer not null, key_id integer);',
'create table keys ( key_id integer autoincrement primary key not null, key blob not null);',
            ],          );
 
 print (Dumper %db_schema_versions);
 print "\n";
 print (Dumper $db_schema_versions{1});
